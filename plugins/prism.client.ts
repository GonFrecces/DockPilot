// plugins/prism.client.ts
import Prism from 'prismjs';
import 'prismjs/themes/prism-tomorrow.css'; // Puedes cambiar el tema
import 'prismjs/components/prism-docker'; // Soporte para Dockerfile

export default defineNuxtPlugin(() => {
  // Nada más, solo importar para usar en componentes
});
